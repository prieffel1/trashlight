version: "3.9"

services:
  trashlight:
    image: ghcr.io/prieffel1/trashlight:latest
    container_name: trashlight
    restart: always

    environment:
      - TZ=Europe/Berlin

    volumes:
      - app-data:/app/data

volumes:
  app-data: {}
